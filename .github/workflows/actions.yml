name: CI
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Setup
      run: npm install
      env:
        NPM_AUTH_TOKEN: ${{ secrets.NPM_INSTALL_TOKEN }}
