name: CI
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Setup
      run: npm install
      env:
        NODE_AUTH_TOKEN: ${{secrets.AUTH_TOKEN}}